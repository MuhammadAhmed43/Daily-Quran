# Copy this file to mobile/.env and fill in your deployed API URL.
# After deploying the /api function to Vercel you'll get a URL like
#   https://daily-quran.vercel.app
# Put it here (no trailing slash). Restart Expo with `npx expo start -c` after changing.
EXPO_PUBLIC_API_BASE=
